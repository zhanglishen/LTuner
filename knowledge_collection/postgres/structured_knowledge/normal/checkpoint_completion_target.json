{
  "name": "checkpoint_completion_target",
  "category": "WAL",
  "context": "sighup",
  "unit": null,
  "description": "Target fraction of the checkpoint interval over which PostgreSQL aims to complete flushing dirty buffers to disk.",
  "symptoms": [
    "excessive I/O spikes during checkpoints",
    "increased latency during checkpoint execution",
    "write amplification due to repeated buffer flushing"
  ],
  "dependencies": [
    "checkpoint_timeout",
    "max_wal_size",
    "bgwriter_lru_maxpages"
  ],
  "constraints": {
    "hard_rule": "0 <= value <= 1",
    "min": "0",
    "max": "1"
  },
  "tuning_tips": "Increase toward 0.9–0.95 to spread I/O load and avoid spikes; reduce only if checkpoints consistently finish too early and cause frequent small checkpoints.",
  "default_formula": "0.9"
}