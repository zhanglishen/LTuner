{
  "name": "max_worker_processes",
  "category": "Resource Usage",
  "context": "postmaster",
  "unit": "count",
  "description": "Maximum number of concurrent background worker processes that the PostgreSQL instance can support, including logical replication workers, parallel query workers, and custom background workers.",
  "symptoms": [
    "Parallel query execution disabled",
    "Logical replication lag on standby",
    "Background worker startup failures",
    "Inability to scale parallel scans or joins"
  ],
  "dependencies": [
    "max_parallel_workers",
    "max_parallel_workers_per_gather",
    "max_logical_replication_workers"
  ],
  "constraints": {
    "hard_rule": "value <= 262143 && value >= 0",
    "min": "0",
    "max": "262143"
  },
  "tuning_tips": "Set to at least the number of CPU cores; match or exceed the master's value on standby servers. Increase only if deploying many parallel queries, logical replication, or custom background workers.",
  "default_formula": "CPU_CORES",
  "suggested_values": [
    2
  ],
  "min_value": null,
  "max_value": null,
  "cpu": 2,
  "ram": 7,
  "disk_size": 39,
  "disk_type": "HDD"
}