{
  "name": "work_mem",
  "category": "Resource/Memory",
  "context": "user",
  "type": "integer",
  "unit": "kB",
  "constraints": {
    "min": 64,
    "max": 2147483647,
    "hard_rule": "work_mem * max_connections < total_ram_kb * 2.0"
  },
  "default_formula": "(total_ram_kb - shared_buffers_kb) / (max_connections * 3)",
  "description": "Specifies the amount of memory to be used by internal sort operations and hash tables before writing to temporary disk files.",
  "symptoms": [
    "high temporary file usage",
    "slow sorting operations",
    "disk spill during hash joins",
    "complex query latency"
  ],
  "dependencies": [
    "max_connections",
    "hash_mem_multiplier"
  ],
  "related_knobs": [
    "shared_buffers",
    "temp_buffers",
    "maintenance_work_mem"
  ],
  "tuning_tips": "Increase this value for complex queries doing sorts or hash joins to perform operations in memory. However, be careful as this is a per-operation limit; complex queries may use multiples of this value.",
  "official_snippet": "Note that for a complex query, several sort or hash operations might be running in parallel; each operation will be allowed to use as much memory as this value specifies before it starts to write data into temporary files.",
  "suggested_values": [
    "1 GB",
    "256 MB"
  ],
  "min_value": null,
  "max_value": "1 GB",
  "cpu": 2,
  "ram": 7,
  "disk_size": 39,
  "disk_type": "HDD"
}