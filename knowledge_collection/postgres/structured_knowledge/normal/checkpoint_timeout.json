{
  "name": "checkpoint_timeout",
  "category": "WAL",
  "context": "sighup",
  "unit": "s",
  "description": "Sets the maximum time interval between automatic WAL checkpoints.",
  "symptoms": [
    "Long crash recovery time",
    "I/O spikes during checkpoint bursts",
    "Increased risk of data loss after crash",
    "Poor write throughput under high WAL generation"
  ],
  "dependencies": [
    "max_wal_size",
    "min_wal_size",
    "checkpoint_completion_target"
  ],
  "constraints": {
    "hard_rule": "value >= 30 and value <= 86400",
    "min": "30",
    "max": "86400"
  },
  "tuning_tips": "Increase to reduce I/O pressure and checkpoint frequency if recovery time tolerance allows; decrease to limit potential data loss and shorten recovery, especially under high WAL volume.",
  "default_formula": "300"
}